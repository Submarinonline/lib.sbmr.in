{
    "name": "lib.sbmr.in",
    "repository": "git@github.com:DevSubmarinonline/lib.sbmr.in",
    "author": "YamaD <yamad@submarin.online>",
    "license": "MIT",
    "devDependencies": {
        "cjp": "latest",
        "parcel-bundler": "1.x"
    },
    "scripts": {
        "build": "npm run build:static && npm run build:cjp",
        "build:static": "mkdir -p pub && cp -r static/* pub/",
        "build:cjp": "parcel build -d pub src/*.js"
    }
}
